package it.polimi.it.galaxytrucker.commands;

public enum RequestType {
    // Default value
    EMPTY,
    NEW_TILE,
    SAVED_TILES,
    DISCARDED_TILES,
    CARD_PILE,
    PLACE_COMPONENT,
    DISCARD_COMPONENT,
    SAVE_COMPONENT,
    SHIP_BOARD,
    SELECT_SAVED_TILE,
    SELECT_DISCARDED_TILE
}
